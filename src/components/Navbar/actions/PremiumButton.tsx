
export function PremiumButton({ }) {
  return (
    <button
      className='rounded-full bg-background px-4.5 py-1 hover:scale-105 transition cursor-pointer'>
      <span
        className='truncate text-black font-semibold text-sm'>
        Explore Premium
      </span>
    </button>)
}
